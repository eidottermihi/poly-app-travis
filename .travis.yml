sudo: required
dist: trusty
addons:
  firefox: 'latest'
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable
cache:
  directories:
    - node_modules/
language: node_js
node_js:
  - "node"
  - "8"
script:
- xvfb-run npm run test-build